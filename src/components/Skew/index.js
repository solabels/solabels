import styled from "@emotion/styled"

const Skew = styled.section`
  transform: skew(0deg, -4deg);
  margin-top: -4%;
  padding-top: 4%;
  > div {
    transform: skew(0deg, 4deg);
  }
`

export default Skew